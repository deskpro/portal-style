language: node_js
node_js:
- '8'
script:
- npm run travis-test
before_deploy:
- npm run build-storybook
deploy:
  - provider: pages
    skip_cleanup: true
    github_token: $GH_TOKEN
    keep-history: true
    local-dir: .site
    on:
      branch: master